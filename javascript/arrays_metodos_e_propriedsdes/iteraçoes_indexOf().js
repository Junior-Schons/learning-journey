//metodo indexOf metodo pracura um valor de elemento em uma matriz e retorna sua posiçao.
const frutas = ['laranja', 'maça', 'limao'];
let position = frutas.indexOf('maça');

console.log(position);
/*
quando o item nao é encontrado o console retornara a posiçao '-1', se houver mais de um valor dentro do array ele retornara a primeira ocorrencia.

podemos usar o lastIndexOf() para retornar a ultima ocorrencia do elemento.
*/

